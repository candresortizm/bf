<!DOCTYPE html>
<html>
  <head>
    <title>Best Friends</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <header>
      <aside><a href="<%= static_pages_welcome_path %>"><img src="<%= image_path('logo_dog.png') %>" alt="Logo_pagina"></a><span><%= current_user ? " #{current_user.email}" : '' %></span></aside>
      <nav>
        <ul>
          <% if !current_user %>
            <li><a href="<%= new_quertionary_path %>">Adopter</li></a>
            <li id="registrar"><a href="<%=new_admin_form_path %>">Fundations</li></a>
          <% else %>
              <li><a href="<%= adopter_signed_in? ? destroy_adopter_session_path : destroy_fundation_session_path  %>" data-method="delete">Log out</a></li>
              <li><a href="<%=adopter_signed_in? ? new_quertionary_path :  admin_forms_path %>"><%=adopter_signed_in? ? "Show all" : "Show All"%></li></a>
          <% end %>
        </ul>
      </nav>
      </header>

    <!--<% if flash[:success] or flash[:error] or flash[:notice]%>
    <span class="flash-message <%= flash[:success] ? 'success' : flash[:error] ? 'error' : flash[:notice] ? 'notice' : '' %>">
      <%= flash[:success] ? flash[:success] : flash[:error] ? flash[:error] : flash[:notice] ? flash[:notice] : '' %>
    </span>

    <% end %>-->
      <div id="msjReques"></div>
    <%= yield %>
  </body>
</html>
